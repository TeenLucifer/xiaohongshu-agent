# 000 Foundation Tasks

## 开发子步骤

- [ ] `000-A`：配置与环境变量基线
  覆盖配置模型、环境变量加载和最小测试。
- [ ] `000-B`：日志与异常基线
  覆盖统一 logger 和基础异常。
- [ ] `000-C`：目录与测试骨架确认
  覆盖 `app/services/schemas/llm/storage` 目录可提交状态，以及最小 smoke test。

## 测试优先任务

- [ ] 为配置模型补失败测试
- [ ] 为日志与异常补失败测试

## 实现任务

- [ ] 建立 `config/`、`utils/` 最小实现
- [ ] 建立 `app/`、`services/`、`schemas/`、`llm/`、`storage/` 最小包骨架
- [ ] 通过质量门禁

## 依赖
- 无业务依赖，按模板默认工程栈执行

## 备注
- 该 feature 完成后，新项目应优先开始第一个真实业务 feature，而不是继续膨胀基础设施。

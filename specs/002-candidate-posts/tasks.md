# 002 Candidate Posts Tasks

## 当前任务分组

- [x] `002-A`：完成右侧搜索结果区建模
  搜索结果、帖子详情弹窗、已选状态和人工顺序都已经收敛到统一前端模型。
- [x] `002-B`：完成极简缩略卡展示
  右侧帖子区采用小图 + 一行标题的紧凑卡片，不再做大卡片和独立已选区。
- [x] `002-C`：完成帖子详情弹窗
  支持查看完整内容、加入已选、移出已选和人工顺序调整。
- [x] `002-D`：完成右侧结果区密度收敛
  搜索结果按紧凑横向顺序排列，右栏变宽后仍保持高密度扫读而不是放大卡片。
- [x] `002-E`：完成真实首图与交互反馈接入
  第一张候选帖使用本地参考图，并统一 hover、选中态和卡片内顺序标记。
- [x] `002-F`：完成弹窗关闭链路与基础动效
  支持关闭按钮、遮罩和 `Esc` 关闭，并保证关闭后页面可继续操作。

## 测试与验收

- [x] 搜索结果卡片渲染测试通过
- [x] 真实图片首图渲染测试通过
- [x] 弹窗开合与关闭链路测试通过
- [x] 已选状态与人工顺序测试通过
- [x] 卡片紧凑密度与横向顺序排列测试通过
- [x] Tailwind + 基础组件表达测试通过

## 实现收口

- [x] 右侧帖子工作区、弹窗和本地状态已落地
- [x] 已选与顺序表达已并入卡片和弹窗
- [x] `acceptance.md` 已与当前实现同步

## 依赖

- 依赖 `001-topic-workspace` 已完成

## 备注

- 当前不做批量操作和复杂筛选器。

# 002 Candidate Posts Tasks

## 开发子步骤

- [x] `002-A`：重定义右侧帖子工作区模型
  覆盖搜索结果卡片、帖子详情弹窗和已选帖子顺序。
- [x] `002-B`：实现搜索结果卡片区
  采用图片优先、标题在下的展示方式。
- [x] `002-C`：实现帖子详情弹窗
  展示完整文案、链接和基础信息，并支持加入已选/移出已选。
- [x] `002-D`：实现已选帖子列表与排序
  让已选帖子顺序保留在本地状态中。
- [x] `002-E`：接入真实图片首图测试
  使用本地参考图验证卡片裁切和标题显示效果。
- [x] `002-F`：补齐弹窗完整关闭链路
  支持关闭按钮、点击遮罩和 `Esc` 关闭。
- [x] `002-G`：统一帖子卡片轻量视觉反馈
  补齐 hover、选中态、图片裁切和标题压缩的一致样式。
- [x] `002-H`：补齐弹窗开合动效与稳定性
  确保打开和关闭都有过渡，关闭后页面仍可继续操作。
- [x] `002-I`：压缩搜索结果卡片体量
  将帖子缩略卡片缩到当前实现的约 `1/4 ~ 1/5`，提高快速扫图密度。
- [x] `002-J`：重排右侧帖子区层级
  让搜索结果成为绝对主区，并移除独立大块的已选顺序区。
- [x] `002-K`：将已选和顺序信息并入搜索结果卡片
  通过轻量标记表达已选状态和人工顺序，不再单独做一块列表。
- [x] `002-L`：将右侧搜索结果区迁移到 Tailwind + 基础组件表达
  统一卡片、弹窗和轻标记的基础实现方式。
- [x] `002-M`：将搜索结果改成更紧凑的横向顺序排列
  减少卡片之间的留白，按从左到右、从上到下的连续节奏展示。

## 测试优先任务

- [x] 为搜索结果卡片渲染补测试
- [x] 为详情弹窗开合补测试
- [x] 为加入已选状态变更补测试
- [x] 为已选帖子排序补测试
- [x] 为真实图片首图渲染补测试
- [x] 为关闭按钮/遮罩/Esc 关闭补测试
- [x] 为卡片 hover 和选中态样式补测试
- [x] 为弹窗关闭后继续操作页面补测试
- [x] 为缩小后的卡片密度补测试
- [x] 为已选和顺序内嵌展示补测试
- [x] 为右侧搜索结果区的 Tailwind 化表达补测试
- [x] 为横向顺序排列和紧凑间距补测试

## 实现任务

- [x] 实现右侧帖子工作区组件
- [x] 接入新的 Mock 搜索结果和已选数据
- [x] 更新 acceptance
- [x] 接入本地参考图首图
- [x] 更新弹窗关闭交互与验收
- [x] 更新卡片样式和弹窗动效实现
- [x] 更新搜索结果密度和右侧区块层级实现
- [x] 更新已选和顺序的卡片内嵌表达
- [x] 更新右侧搜索结果区的 Tailwind + 基础组件实现
- [x] 更新搜索结果的横向顺序排列与紧凑间距实现

## 依赖

- 依赖 `001-topic-workspace` 已完成

## 备注

- 当前不做批量操作和复杂筛选器。

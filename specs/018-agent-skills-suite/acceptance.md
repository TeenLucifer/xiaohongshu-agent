# 018 Agent Skills Suite Acceptance

1. `pattern-summary` 默认消费当前全部剩余帖子
2. `copy-rewrite` 默认消费当前全部剩余帖子与总结结果
3. `image-analysis` 与 `image-generation` 只读取各自 skill 目录下的 `config.json`
4. skill 文档中不再把 `selected_posts.json` 作为正式输入

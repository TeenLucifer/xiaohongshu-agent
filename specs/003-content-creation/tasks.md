# 003 Content Creation Tasks

## 当前任务分组

- [x] `003-A`：完成内容创作展示模型
  已拆分中栏对话消息和右侧工作区内容分组的数据形状。
- [x] `003-B`：完成总结与文案消息接入
  模式总结和文案结果都作为中栏中的 agent 回复消息展示。
- [x] `003-C`：完成文案编辑态
  文案消息已收口为只读展示，正式编辑入口固定在右侧工作区。
- [x] `003-D`：完成右侧文案与图片区
  文案摘要和图片结果按折叠分组展示，保持辅助区定位。
- [x] `003-E`：完成内容结果视觉收敛
  去掉主界面摘要块，统一中栏消息密度、右栏摘要样式和整体轻量表达。
- [x] `003-F`：完成 Tailwind + 基础组件接入
  文案摘要、图片分组和编辑态已统一到当前前端基础组件体系。
- [x] `003-G`：完成右侧“总结/文案”触发按钮
  两个按钮复用现有 run 链路，作为固定动作入口。
- [x] `003-H`：完成真实总结/文案结果接入
  `pattern_summary.json` 与 `copy_draft.json` 已切到真实读取，不再依赖 mock。
- [x] `003-I`：完成右侧总结 Markdown 展示
  总结区改为 Markdown 只读渲染，并兼容结构化字段合成正文。
- [x] `003-L`：完成右侧文案单文档 Markdown 编辑
  右侧文案区改为单编辑框文档体验，支持 Markdown 语法识别、Enter 提交当前行并真实写回。
- [x] `003-M`：完成文案区选区 AI 润色入口与内部滚动
  右侧文案区支持选中后输入提示词触发局部润色，并由编辑器内部滚动承接长文输入。
- [x] `003-N`：完成 workspace-first 内容分布调整
  总结、文案、图片继续留在 `选题 / 创作` tab 中，对话结果统一迁入 `对话` tab。
- [x] `003-O`：完成图片区局部输入接入
  图片 section 已支持局部输入，消息复用统一 run 链路并同步进入 `对话` tab。

## 测试与验收

- [x] 总结消息渲染测试通过
- [x] 文案主栏只读展示测试通过
- [x] 右侧文案摘要与图片分组测试通过
- [x] 主界面无结果摘要块测试通过
- [x] 中栏消息密度与右栏降权测试通过
- [x] Tailwind + 基础组件表达测试通过
- [x] 总结/文案触发按钮测试通过
- [x] 真实总结/文案读取测试通过
- [x] 右侧总结 Markdown 展示测试通过
- [x] 右侧文案单文档 Markdown 编辑与写回测试通过
- [x] 文案区选区 AI 润色入口与内部滚动测试通过
- [x] `选题 / 创作` 触发 run 后停留当前 tab 测试通过
- [x] 图片区局部输入发送并同步对话记录测试通过

## 实现收口

- [x] 内容创作相关消息节点和右侧分组已落地
- [x] 总结与文案结果已接入真实 workspace 读取
- [x] `acceptance.md` 已与当前实现同步

## 依赖

- 依赖 `001-topic-workspace` 已完成

- [x] `003-J`：接入真实图片结果展示区
  由 `025-image-generation-skill` 提供真实 `image_results`，在右侧图片 section 下半部分展示。
- [x] `003-K`：支持 final answer 图片缩略图
  中栏最终回复可附带 1 张代表图，和右侧结果区引用同一份图片真相层。

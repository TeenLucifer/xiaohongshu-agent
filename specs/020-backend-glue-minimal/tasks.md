# 020 Backend Glue Minimal Tasks

## 当前状态

- [x] `020-backend-glue-minimal` 已完成首版实现，当前处于联调与后续扩展阶段。

- [x] `topic_id -> session_id` 文件化映射已落地
- [x] `GET /workspace`、`POST /runs`、`GET /messages`、`POST /reset` 已落地
- [x] `GET /context` 与工作区对象读取已落地
- [x] 文案选区 AI 润色专用接口已落地
- [x] 编辑区图片写回与图片区上传图片接口已落地
- [x] 统一错误 DTO、最小 CORS 与前后端联调测试已补齐

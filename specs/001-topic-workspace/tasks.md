# 001 Topic Workspace Tasks

## 当前任务分组

- [x] `001-A`：完成三栏工作台骨架
  默认直达工作台，固定左侧导航、中间对话主栏、右侧工作区的整体布局。
- [x] `001-B`：完成三栏宽度、滚动与收起联动
  固定桌面端列宽比例，支持左栏和右栏独立收起，并保证三栏独立滚动。
- [x] `001-C`：完成工作台主次关系收敛
  让中栏成为唯一主焦点，压低头部信息和输入区权重，并让右栏退为辅助上下文区。
- [x] `001-D`：完成左侧导航定型
  左栏贴住页面左侧并铺满视口高度，收起后保留品牌和主导航图标列，当前项保持高亮。
- [x] `001-E`：完成统一视觉基线接入
  切到 Tailwind + 轻量基础组件体系，并统一字号、边框、阴影、图标和容器级动效。
- [x] `001-F`：完成页面级 Mock 状态覆盖
  为工作台主要区域保留空、加载、成功、失败的前端展示能力。

## 测试与验收

- [x] 默认进入工作台测试通过
- [x] 三栏布局与列宽比例测试通过
- [x] 左栏与右栏收起联动测试通过
- [x] 三栏独立滚动测试通过
- [x] 左栏贴边、满高、无圆角测试通过
- [x] 轻量输入区与中栏主焦点测试通过
- [x] 基础组件、结构型图标与容器级动效测试通过

## 实现收口

- [x] 工作台容器、布局状态与 Mock 数据已落地
- [x] 左栏、中栏、右栏的最终形态已落地
- [x] `acceptance.md` 已与当前实现同步

## 依赖

- 依赖 `000-foundation` 已完成

## 备注

- 当前 feature 只处理前端工作台结构，不接真实接口。

# 001 Topic Workspace Tasks

## 当前任务分组

- [x] `001-A`：完成 workspace-first 布局重构  
  页面已收敛为“左侧导航 + 单一工作区主面板”，不再保留中栏/右栏双内容列。
- [x] `001-B`：完成三 tab 工作区  
  主工作区已提供 `选题 / 创作 / 对话` 三个 tab，并以此承接原中栏与右栏内容。
- [x] `001-C`：完成对话区降级为结果记录 tab  
  对话消息流与输入框已迁入 `对话` tab，不再作为页面中心主栏常驻。
- [x] `001-D`：完成左侧导航与主工作区宽度联动  
  左侧收起后，主工作区直接扩展占满剩余宽度。
- [x] `001-E`：完成 tab 内工作流收口  
  `选题 / 创作` 中触发 run 的按钮保持停留在当前 tab，结果记录进入 `对话` tab。
- [x] `001-F`：完成 section 头部与局部输入收口  
  section 头部已去掉状态 badge，`选题 / 创作` 中的局部输入框也已接入统一 run 链路并写入 `对话` tab。

## 测试与验收

- [x] workspace-first 布局测试通过
- [x] 三个工作区 tab 渲染测试通过
- [x] 对话输入框只在 `对话` tab 中显示测试通过
- [x] 左栏收起后主工作区扩展测试通过
- [x] `选题 / 创作` 触发 run 后不自动跳转到 `对话` tab 的交互测试通过
- [x] `选题 / 创作` 局部输入写入统一对话记录测试通过

## 实现收口

- [x] 工作台主结构、tab 组织和对话区位置已按新口径落地
- [x] `acceptance.md` 已与当前实现同步

## 依赖

- 依赖 `000-foundation` 已完成

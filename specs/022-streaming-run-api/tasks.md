# 022 Streaming Run API Tasks

## 当前状态

- [x] `022-streaming-run-api` 首版已实现：后端提供 SSE run 事件流，前端默认走流式并保留同步兜底。

## 已确认里程碑

- [x] 固定 `022` 依赖 `020` 的既有后端入口与 topic/session 映射。
- [x] 固定同步 `POST /runs` 继续保留，流式能力并行新增。
- [x] 固定第一版只服务主栏运行过程，不覆盖完整 topic 真相层更新。
- [x] 固定首版至少需要开始、增量、工具调用、完成、失败事件。

## 当前待办

- [x] 确定首版流式传输方式为 SSE。
- [x] 定义流式事件协议与事件字段。
- [x] 实现流式 run 服务入口。
- [x] 实现同步/流式结果对齐策略。
- [x] 实现前端主栏的流式消费与渐进展示。
- [x] 补齐流式事件、失败路径与回归测试。

## 后续可继续优化

- [ ] 将 `assistant_delta` 从“最终文本快速回放”升级为 provider 原生 streaming。
- [ ] 优化运行中 tool 摘要和失败态的视觉样式。

## 备注

- 详细边界与验收要求以下列文档为准：`spec.md`、`acceptance.md`
